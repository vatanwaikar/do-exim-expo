import GalleryPage from '@/components/pages/GalleryPage';

export const metadata = {
  title: 'Gallery – DoExim Expo',
  description: 'Preview the premium exhibit gallery for DoExim Expo.',
};

export default function Page() {
  return <GalleryPage />;
}
